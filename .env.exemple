# --- API (obligatoire) ---
OPENAI_API_KEY=__REPLACE_ME__

# --- API (optionnel) ---
# Laisse vide si tu utilises l'API OpenAI standard. Renseigne l'URL si tu es sur Azure/proxy.
OPENAI_API_BASE=
# Modèle par défaut (tu peux changer sans toucher au code)
MODEL=gpt-4o-mini

# --- Identité affichée dans la lettre ---
USER_FULLNAME=Your Name
USER_ADDRESS=Street & Number
USER_CITY_COUNTRY=City – Country
USER_PHONE=+00 0 00 00 00 00
USER_EMAIL=your.email@example.com

# --- Mise en page DOCX (optionnel) ---
DOC_FONT=Aptos (Body)
DOC_FONT_SIZE_PT=12
DOC_LINE_SPACING=1.22
DOC_MARGINS_INCH=1.00,0.60,1.10,1.10
DOC_SPACE_AFTER_EMAIL_PT=12
DOC_SPACE_AFTER_SUBJECT_PT=12
DOC_SPACE_AFTER_SALUT_PT=12
DOC_SPACE_AFTER_PAR_PT=6
DOC_SPACE_AFTER_SIGNATURE_PT=12

# --- Sortie ---
OUTPUT_DIR=generated_letters
